let initialize = {
    testik:
    [{id:1, name: "pupsik"}],
}


const navbarReducer = (state = initialize, action) => {
    switch (action.type) {
        default:
            return state;
    }
}


export default navbarReducer;